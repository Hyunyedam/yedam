# 1. print()_함수
#     정의 : 출력할떄 쓰이는 함수
#     사용 방법 : print()의 ()안에 출력을 할 것을 ""와 함꼐 적는다
#
# 2. type() int() str() float() 함수
#     정의 : 출력하는 값이 무슨 형태인지 알 수 있다
#     사용 방법 : print()와 같이 쓰일 수도 있고 () 안에 정수를 입력하여 쓰일 수 있다
#
# 3. 인덱싱
#     정의 : 리스트 등의 어떤것에 번호 등을 통해 순서를 나타내는 것이다
#
# 4. 슬라이싱
#     정의 : 리스트에 있는 것을 일부만 가져오는 것이다
#     사용 방법 : [ : ] 형태로 사용할 수 있다
#
# 5. replace() 함수
#     정의 : 저장한 텍스트나 인덱스부터 문자열을 다른 문자열로 바꿔준다
#     사용 방법 : 문자나 특정 문자를 바꿔야 할 때 사용한다